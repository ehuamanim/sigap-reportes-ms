{
  "name": "sigap-ms-reportes-be",
  "version": "1.0.0",
  "description": "Microservicio para reportes",
  "scripts": {
    "build": "tsc && webpack --mode production",
    "package": "npm run build && cd dist && zip -r lambda.zip .",
    "deploy:infra": "cd infrastructure && npm run deploy",
    "deploy:lambda": "npm run package && aws lambda update-function-code --function-name UserMicroserviceStack-UserLambdaFunction --zip-file fileb://dist/lambda.zip",
    "deploy": "npm run deploy:infra && npm run deploy:lambda",
    "test": "jest"
  },
  "author": "Ericson Huamaní",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^24.3.1",
    "@types/pg": "^8.15.5",
    "ts-loader": "^9.5.4",
    "typescript": "^5.9.2",
    "webpack": "^5.101.3",
    "webpack-cli": "^6.0.1"
  }
}
